<div class="container">
	<div class="row">
  		<div class="panel">
<%= form_for(@notice) do |f| %>
  <% if @notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notice.errors.count, "error") %> prohibited this notice from being saved:</h2>

      <ul>
      <% @notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
<table class="type02">
  <tr>
  <div class="field">
    <th scope="row"><%= f.label :제목 %></th>
    <td><%= f.text_field :notice_title, size:70 %></td>
  </div>
  </tr>
  <tr>
  <div class="field">
    <th scope="row"><%= f.label :내용 %></td>
    <td><%= f.text_area :notice_content, cols:80, rows:30 %></td>
  </div>
  </tr>
</table>
<center>
  <div class="actions">
    <%= f.submit "확인", class: 'btn btn-default', align:"center" %>
  </div>
</center>
<% end %>
      </div>
  </div> 
</div>
